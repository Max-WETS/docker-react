language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t stewmix/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true stewmix/docker-react npm run test
